import CardHeader from "./CardHeader";
import CardBody from "./CardBody";

const Card = () => {
  return (
    <div className="card">
      <CardHeader />
      <CardBody />
    </div>
  );
};

export default Card;
